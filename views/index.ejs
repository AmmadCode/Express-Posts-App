<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Posts</title>
    <link rel="stylesheet" href="/style.css">
</head>

<body>
    <h1>Quora Posts</h1>
    <div class="posts-container">
        <% for(post of posts) { %>
            <div class="post">
                <h3 class="user">@<%= post.user %></h3>
                <h4 class="content"><%= post.content %></h4>
                <a id="ab" href="http://localhost:8080/posts/<%= post.id %>">See in details</a>
                
                <div class="btns">
                    <a id="special"  href="http://localhost:8080/posts/<%= post.id %>/edit">Edit</a>
                    <form method="POST" action="/posts/<%= post.id %>?_method=DELETE">
                        <button style="background-color: rgb(246, 38, 38);">Delete Post</button>
                    </form>
                    
                </div>

            </div>
            
        <% } %>
    </div>
    


    <a id="a" href='http://localhost:8080/posts/new'>Create New Post</a>


</body>

</html>